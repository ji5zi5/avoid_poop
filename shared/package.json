{
  "name": "shared",
  "private": true,
  "dependencies": {
    "zod": "^4.1.5"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/**/*.test.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
