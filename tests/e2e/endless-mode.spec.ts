import {test} from '@playwright/test';

test.describe.skip('endless mode', () => {
  test('plays endless mode and stores a record', async () => {
    // Filled in after app wiring is complete.
  });
});
