import {test} from '@playwright/test';

test.describe.skip('round mode', () => {
  test('plays through a round and boss pattern', async () => {
    // Filled in after app wiring is complete.
  });
});
