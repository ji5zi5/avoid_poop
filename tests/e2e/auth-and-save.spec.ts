import {test} from '@playwright/test';

test.describe.skip('auth and save', () => {
  test('signup, play, save, and view records', async () => {
    // Filled in after app wiring is complete.
  });
});
