{
  "name": "avoid-poop",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "frontend"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:frontend",
    "dev:server": "npm run dev --workspace server",
    "dev:frontend": "npm run dev --workspace frontend",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace frontend",
    "test": "npm run test --workspace shared && npm run test --workspace server && npm run test --workspace frontend && node --import tsx tests/e2e/run-e2e.mjs",
    "lint": "npm run lint --workspace shared && npm run lint --workspace server && npm run lint --workspace frontend",
    "e2e": "node --import tsx tests/e2e/run-e2e.mjs"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3"
  }
}
